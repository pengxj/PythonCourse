import tkinter
from tkinter import *


root = tkinter.Tk()

label1 = tkinter.Label(root, text="SZTU")
label1.pack(side=TOP)
bnt = tkinter.Button(root, text="click")
bnt.pack(side=TOP)

root.mainloop()
